# 프로젝트 품평과 최적화 기준

작성일은 2026-05-09입니다.

## 현재 총평

이 프로젝트는 개인 영어 문장 암기용 정적 웹앱이라는 목적에 잘 맞게 구성되어 있다. 서버 없이 `index.html`, `style.css`, `script.js`, `data.json`만으로 실행되고, 학습 코스 전환, Day 선택, 카드 이동, 음성 읽기, 다크 모드, 글자 크기, 복습 통계가 한 화면 안에서 끝난다.

현재 구조의 장점은 단순함이다. 외부 프레임워크가 없고, 데이터도 정적 JSON이라 GitHub Pages 배포와 로컬 실행이 쉽다. 반대로 단점은 모든 UI 로직이 `script.js` 한 파일에 모여 있고, 학습 데이터의 원천이 Notion인데 사이트 데이터는 `data.json`에 수동 반영해야 한다는 점이다. 앞으로 가장 큰 위험은 코드 복잡도보다 데이터 동기화 실수와 회귀 검증 누락이다.

현재 규모는 대략 `script.js` 745줄, `style.css` 404줄, `index.html` 94줄, `data.json` 96KB다. 기능 수에 비하면 과하지 않지만, `script.js`는 마지막으로 더 커지기 전에 기준을 잡아두는 것이 좋다.

## 평가 기준

앞으로 개선 여부는 아래 순서로 판단한다.

1. 학습 흐름 안정성.
   Day 안의 모든 카드가 순서대로 진행되고, 마지막 카드에서만 복습 완료가 보이며, 이전/다음 버튼이 Day 경계를 올바르게 넘는지 본다.

2. 데이터 신뢰성.
   Notion 원문, `data.json`, 실제 GitHub Pages 화면의 내용이 같은지 확인한다. 오타, 빈 Day, 한 장짜리 임시 Day, 캐시된 예전 파일이 가장 큰 리스크다.

3. 사용자 설정 보존.
   `localStorage`에 저장되는 글자 크기, 다크 모드, 음성 선택, 복습 통계가 코스 전환과 배포 후에도 유지되는지 본다.

4. 코드 단순성.
   한 번만 쓰는 추상화는 만들지 않는다. 대신 같은 패턴이 두 번 이상 반복되고 수정 이유가 명확할 때만 공통화한다.

5. 배포 검증 가능성.
   수정 후 `node --check script.js`, 데이터 수 검증, `git diff --check`, GitHub Pages 공개 URL 확인까지 짧게 반복할 수 있어야 한다.

## 코드 축소 후보

아래 항목은 줄일 수 있지만, 바로 기능을 바꾸기보다 우선순위를 두고 처리하는 편이 안전하다.

| 우선순위 | 후보 | 판단 |
|---|---|---|
| 높음 | `script.js`의 중복되거나 장황한 주석 | 2026-05-09에 적용했다. 중복된 `populateVoiceList` 설명과 단순한 설명성 주석을 줄였다. |
| 높음 | `index.html`의 미사용 클래스 `card`, `card-content` | 2026-05-09에 적용했다. CSS와 JS에서 쓰지 않는 클래스만 제거했다. |
| 중간 | 설정 모달과 통계 모달의 열기/닫기 로직 | `openSettings`, `closeSettings`, `openStats`, `closeStats`가 비슷하다. 공통 헬퍼로 줄일 수 있지만, 모달별 동작 차이가 생길 때는 분리 유지가 낫다. |
| 중간 | TTS 음성 옵션 생성 로직 | `addOptions`, 표시 이름 정리, 빈 옵션 생성을 더 작게 만들 수 있다. 단, 브라우저별 음성 로딩이 예민하므로 기능 검증이 필요하다. |
| 중간 | 통계 테이블 렌더링의 인라인 스타일 | CSS 클래스로 옮기면 유지보수성은 좋아진다. 총 줄 수는 크게 줄지 않을 수 있다. |
| 낮음 | `ReviewManager`의 레거시 `reviewStats` 이어받기 | 기존 사용자의 영어회화 복습 통계를 보존하기 위한 코드다. 충분히 시간이 지난 뒤에만 제거한다. |
| 낮음 | 관리자 클래스를 단순 함수로 병합 | 줄 수는 줄 수 있지만 책임 분리가 흐려진다. 현재는 유지하는 편이 낫다. |

## 줄이지 않는 것이 나은 부분

`FontSizeManager`, `DarkModeManager`, `TTSManager`, `ReviewManager`, `QuizApp`의 클래스 분리는 현재 기능 경계와 잘 맞는다. 줄 수만 줄이기 위해 하나로 합치면 이후 수정이 더 어려워질 가능성이 크다.

Day 진행 로직은 현재 명시적인 편이 좋다. 특히 기본동사처럼 하루에 여러 카드가 들어가는 데이터에서는 `handlePrev`, `handleNext`, `loadDay`, `updateCard`의 책임이 분리되어 있어 문제 추적이 쉽다.

코스별 복습 통계 키인 `reviewStats:<courseId>`도 유지하는 편이 좋다. 영어회화와 기본동사가 같은 Day 이름을 쓰기 때문에 통계를 합치면 사용자 기록이 섞인다.

## 개선 계획

1. 데이터 동기화 절차를 먼저 안정화한다.
   Notion을 원천으로 보고 `data.json`을 생성하거나 검증하는 절차를 문서화한다. 최소한 Notion 수정 후에는 `data.json` 카드 수, 빈 Day, 대표 문장, 오타 후보를 확인한다.

2. 작은 무해 리팩토링을 먼저 한다.
   미사용 HTML 클래스 제거, 중복 주석 정리, 인라인 스타일의 CSS 이동처럼 동작 영향이 거의 없는 변경부터 처리한다.

3. 모달 공통화는 한 번에 하나만 한다.
   설정 모달과 통계 모달의 열기/닫기 로직을 공통 함수로 줄일 수 있다. 다만 이 작업은 화면 동작 검증이 필요하므로 별도 커밋으로 분리한다.

4. TTS는 마지막에 건드린다.
   브라우저마다 음성 목록 로딩 타이밍이 달라서 회귀가 생기기 쉽다. 음성 설정 저장, 테스트 음성, 한국어 질문 자동 읽기, 영어 정답 읽기를 모두 확인할 준비가 됐을 때만 줄인다.

5. 기능 추가보다 검증 루틴을 먼저 만든다.
   현재 앱은 테스트 프레임워크가 없으므로 최소 검증 스크립트를 두는 것이 좋다. 예를 들어 `data.json`의 코스 수, Day 수, 카드 수, 빈 q/a, 빈 Day, 중복 Day를 검사하는 Node 스크립트를 추가할 수 있다.

## 변경 전 체크리스트

- `git status --short`로 작업트리가 깨끗한지 확인한다.
- 변경 범위를 JS, CSS, 데이터, 문서 중 하나로 좁힌다.
- `checklist.md`에 작업 항목을 추가한다.
- `context-notes.md`에 판단 이유를 남긴다.
- Notion 내용과 `data.json` 중 어느 쪽을 기준으로 삼는지 명확히 한다.

## 변경 후 체크리스트

- `node --check script.js`를 실행한다.
- `data.json`을 파싱해 코스별 Day 수와 카드 수를 확인한다.
- `git diff --check`로 공백 오류를 확인한다.
- 화면 변경이 있으면 로컬 브라우저에서 코스 전환, Day 이동, 마지막 카드 복습 완료, 통계 모달을 확인한다.
- GitHub Pages 배포 후 공개 URL에서 캐시 버전과 실제 동작을 확인한다.

## 현재 결론

지금 당장 큰 구조 변경은 필요하지 않다. 프로젝트의 목적과 규모를 생각하면 현재 구조는 충분히 합리적이다. 다음 개선은 기능 추가보다 데이터 동기화 안정화와 작은 코드 정리부터 하는 것이 좋다. 줄 수를 많이 줄이는 것보다 실수 가능성을 줄이는 방향이 이 프로젝트에는 더 잘 맞는다.
